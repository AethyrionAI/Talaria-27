# HANDOFF — Hermes 0.19 "Quicksilver" follow-through (2026-07-20 late)

**From:** the launch-pass session (OPEN_ITEMS owner tonight). **Context:** long; handing
off the remaining #148 work per Owen. Read OPEN_ITEMS #148 first — it is canonical and
carries tonight's verdicts. This doc is orientation only.

## State as of handoff
- Both hosts on Hermes v0.19.0. **Mac gateway VERIFIED running 0.19** (process start 20:43,
  post-update). **OJAMD gateway NOT verified** — Owen's update restarts NSSM services only;
  gateway pythonw + connector may still run pre-0.19 code. The sibling session
  (claude.ai/chat/ff8aaa89-05b9-469e-9a30-34ce8143ce10) owns the OJAMD half; coordinate,
  don't duplicate. Single-writer rule for OPEN_ITEMS is in force — confirm with Owen who
  owns the file in your session.
- **SSE capture DONE (Mac, /tmp/sse-019.log): 0.19 wire fully compatible.** Taxonomy
  identical to CLEAN_CHAT_PATH.md; reasoning stays on tool.progress/_thinking; answer
  chunks clean. No app changes needed. Do NOT redo this on the Mac.
- Toolchain: Xcode-beta4 (/Applications/Xcode-beta4.app), DEVELOPER_DIR required in every
  shell. Suite baseline 931/84 green on b4 SDK. Sim UDID pin unchanged.

## The remaining #148 work, in priority order
1. **model_routes eval doc (resolves the #116 hold).** Compare tools/models-shim/shim.py's
   surface (list models, set global default, dual-token auth) against 0.19 native:
   `model_routes` map in platforms.api_server.extra (per-client routing on
   /v1/chat/completions, /v1/responses, /v1/runs; precedence session /model → route →
   global), durable per-session /model (#57030), GET /v1/models alias discovery. Deliver:
   keep/trim/retire recommendation + app-side migration sketch (models plane currently
   talks to shim :8765). Verdict lands in #116 AND #148. The Mac gateway is live for
   empirical probes (key: API_SERVER_KEY in ~/.hermes/.env).
2. **Host-side tool-name check** (mcp__server__tool convention): grep OJAMD + Mac Hermes
   skills/prompts (apple-messaging skill especially) for bare hermes_mobile tool names.
   Rides the next host window.
3. **OJAMD gateway vintage check** (sibling session's thread): process start time vs update
   time; feeds the #143 redelivery-ledger timing discriminator.

## Open decisions parked with Owen (do not self-serve)
- #139 dispatch: written, READY TO SEND (dispatch/FABLE-T27-139-connect-teardown.md).
- #146 fix: confirmed display desync, dispatchable micro-lane (pairs with #139).
- #149 Claude↔Hermes MCP bridge: parked idea, Owen schedules.
- Circle-back stack: #128/#129/#137/#126, #99 WKContentRuleList, #56 routing confirmation.

## Practical cautions (learned tonight, the hard way)
- OPEN_ITEMS edits: python script to /tmp via write_file, run by path. NEVER inline
  heredocs in double-quoted zsh -c (backtick substitution mangled #21 tonight; repaired).
  Always include an idempotency guard (assert marker not in file) — it prevented a forked
  record twice tonight.
- Fresh shells: export DEVELOPER_DIR every time. DC shells die after long ops — one-shot
  zsh -l -c for short commands, persistent PID only when interaction is needed.

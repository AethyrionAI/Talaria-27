# HANDOFF — written 2026-07-09 (late evening), for the next session

**Repo state:** `AethyrionAI/Talaria-27` main @ `75dd6b3`. Working tree clean on `main`
(`planning/` untracked by design). Phone (whoGoesThere) carries `c9e909e` — KEEP IT
(= main's app code + #84 instrumentation; #85–#87 were server-side only).

**Owen has Fable (Claude Code cloud) credits until SUNDAY — maximize well-specified,
cloud-verifiable work. Fable CANNOT reach Tailscale (:8642/:8000) or devices.**

---

## Priority 1 — P1 "brain" transplant-fidelity probe (~1 hr, zero Fable credits)

The existential question for the Continuity Fabric (10x P1), straight from the vision
doc: does a condensed priming turn into a FRESH Hermes session read as continuous?
This gates Fable Lane A (the biggest credit spend). Run BEFORE dispatching Lane A.

Where: Sessions API `http://ojamd:8642` (Tailscale; iCloud Private Relay off).
Auth: `Bearer $API_SERVER_KEY` — lives in `%LOCALAPPDATA%\hermes\.env` on OJAMD
(Owen can paste it, or run the probe from OJAMD DC directly).
Contract: `POST /api/sessions` → id at `.session.id`; turns via
`POST /api/sessions/{id}/chat` (sync, read `.message.content`).

Procedure:
1. Session A: seed 4–6 turns of facts with entanglement (e.g. a project with a
   deadline, a constraint, a correction mid-way, one numeric detail). Then ask A
   3 probe questions (recall, inference across turns, the corrected fact).
2. Condense A's transcript (~10:1) — a summary paragraph as a priming turn.
   (LocalIntelligenceService's condensation helpers are the app-side analog; for
   the probe, hand-condense or ask Hermes itself to summarize.)
3. Session B (fresh): first turn = the condensed priming. Ask the SAME 3 questions.
4. Compare A vs B answers: pass = B indistinguishable on recall + inference and
   respects the mid-conversation correction. Fail modes to watch: B treats priming
   as a quote not context, loses the correction, or hallucinates around gaps.
5. Bonus rung: repeat with a raw replay (no condensation) to bracket fidelity.

Verdict → Lane A GO/NO-GO. Record result in OPEN_ITEMS (new item) either way.

---

## Priority 2 — dispatch Fable lanes (B and C are probe-independent: fire anytime)

**Lane A — P1 Continuity Fabric (GATED on Priority 1 pass).** Journal as the durable
primary object; context transplant at every brain hop; conversation identity decoupled
from `apiSessionId`; offline compose outbox (copy `SensorOutboxState` pattern).
Spec source: 10x doc P1 section (builds-on/conflicts list is near-complete).
Key conflict: `SessionsHermesClient.ensureSession()` single-session assumption
(~lines 371–428). Priming costs real tokens — surface via existing receipts.

**Lane B — Markdown rendering depth (Field Notes §7).** Parser handles only fences +
images (`MarkdownParser.swift:121–177`). Add: tables, headings, block quotes, lists,
syntax highlighting. Pure Swift, unit-test heavy. Daily-life win; compounds the
shipped context-menu/copy work.

**Lane C — correctness batch.** (1) `/save` reports success on silent failure
(`ChatScreen.swift:850–851`, empty catch) + never offers share sheet; (2)
`HapticEngine.error()` defined, never called — failed sends buzz nothing; (3) stale
tracking comment `SessionsSettingsScreen.swift:12–15`; (4) #58 inbox decoder
skip-bad-rows hardening (gh#58); (5) #84 follow-up: preflight third state —
"permissions OK, no mic input — try rebooting" (misclassifies no-input as
permission-denied today; pure logic + unit tests NOW, device verify post-seed).

**Lane D (if credits remain).** P8 IR v0: IR = the existing HUD components exactly;
hardcoded IR-tree renderer; `@Generable` schema. Fable builds all but the on-device
emission-quality test. OR candidates #5 (tool telemetry — P7 prereq) / #11 (composer
draft persistence).

Line references are from the 2026-07-08 discovery @ `a4e2518` — re-confirm at HEAD
in each spec (standard practice).

---

## Priority 3 — OJAMD owes (next time DC is on OJAMD)

1. **#87 connector UTF-8 fix is MERGED but NOT DEPLOYED** — OJAMD rebased before it
   landed. `ojamd-deploy` is now behind ~4 (5a2df40/e2f42ac/ffd92a8/75dd6b3).
   Redo the reconcile: `git fetch t27 && git rebase t27/main` (helper commit replays
   clean — proven tonight), then re-invoke `start-connector.bat` (non-elevated).
   Relay does NOT need a bounce for #87 (connector-only... the fix also touched
   nothing under relay/). Verify: UnicodeDecodeError count in connector.log stops
   growing; `hermes memory status` summaries populate.
2. **#88**: fix `restart-relay.ps1` relay half → `Restart-Service HermesMobileRelay`
   (elevation = Owen pastes); connector half is correct as-is.
3. **#54 possible closure**: tonight's relay restart WAS the post-restart test —
   connector reattached cleanly, no 4401 (relay.log ~17:2x, WS 62532→62610 handoff).
   Verify the server-side nonce persistence code angle, then mark #54 resolved.

---

## Parked / standing state

- **#82 voice wedge: PARKED.** Apple's beta seed breaks ALL third-party capture
  (Discord repro'd it, Talaria-free). Full elimination board in OPEN_ITEMS #82 —
  do not re-litigate. Owen owes Apple Feedback: reboot → Voice Memos works → any
  third-party mic → capture dead. Retest = 10 min on next seed (c9e909e build has
  the route line + Diagnostics panel for it).
- **#84 branch** (`claude/t27-84-talk-preflight` @ c9e909e): green (Xcode 27.0,
  13/13 Swift Testing), UNMERGED — device checklist wedge-blocked + owes the Lane C
  third-state fix. Merge after both.
- **`probe/no-vpio` @ 3d5721e**: pushed, NEVER TESTED, do-not-merge (superseded).
- **`diagnostics/voice-probes`**: scripts valuable; its OPEN_ITEMS #82 closure text
  asserts the falsified permission root cause — REWORK before merging.
- **Toolchain**: ALWAYS `export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/`
  `Developer` (default CLI xcodebuild = 26.6 = wrong-SDK ghost errors; memory #12).
- **10x doc ghost**: its P5 cold-start says "fix #24f first" — #24f is DEAD
  (memory #9); the real transport gate is #54 (possibly closed, see above).
- **Open strategic fork** (Owen sitting with it): shippable standalone App Store app
  vs agent-continuity vision. Tonight's P1 interest leans continuity; the probe
  result will sharpen it. Discord remains one token away. SBP/PCC: entitlement
  request follows SBP approval; PCC already buildable behind availability gate (#30).

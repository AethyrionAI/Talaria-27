# WEEK PLAN 2026-07-29 → 2026-08-02 — the #200 program, prepared for the next executor

**Written 2026-07-29 by the Fable 5 session that ran #200F/G/H, at Owen's
request: Fable credits are limited this week, and Opus 5 takes over when
they run out. This document is the strategy; `OPEN_ITEMS.md` is the
canonical per-item record; the latest local handoff
(`handoffs/HANDOFF-2026-07-29-T27-200-H.md`, gitignored) is the
session-state snapshot. Read all three before acting. Owen directs and
routes every merge and promotion; the executor writes all code and runs
all infrastructure.**

## Mission and state

Get Talaria's on-device action path (Apple FoundationModels, iOS 27 beta 4,
device whoGoesThere) to an acceptable launch percentage. The method that
works — the ONLY method: measured battery cells, one seam per cell, verdict
from raw text, promotion behind a pinned default-flip with a byte-identical
rollback flag.

Lifetime scoreboard (all verdicts in OPEN_ITEMS with dated notes):

- **Remind creates: 0/50 → 75% (15/20 pooled #200G re-verify)** via two
  promoted instruction seams. Latest run's control: 6/10 (band ~60-80%).
- **Alarm: ceiling (10/10) in every cell of every run.** Never regressed.
- **Calendar: ~70% production; 9/10 in #200H's spiralfix cell** (best
  ever) but that cell is NOT promotable (collateral, below).
- **Haiku grabs: ~50% standing in ARMED construction.** IMPORTANT
  framing: production chat is `armed-routed` — a real "write a haiku"
  routes TOOLLESS first, so the ~50% is conditional on a router miss, not
  the raw user experience. It is still a disease; it is not the fire the
  raw number suggests.
- Production instructions = base + `includeActionDestallClause` (TRUE,
  #200D) + `includeFindFirstCarveout` (TRUE, #200G). Each flag's explicit
  `false` is a pinned byte-identical rollback
  (`actionDestallClauseIsProductionDefaultAndRemovable`,
  `findFirstCarveoutIsProductionDefaultAndRemovable`).

Disqualified paths — do not revisit without new evidence: `.required`
tool-calling mode (deterministic decoder crash
`ifpInvalidExpertPickPosition`, 20/20; NO Apple filing, per Owen —
treated as environmental, routed around); belt narrowing / per-intent
scoping (kills grabs but resurrects the #196 composition-denial disease,
haiku cant 10/10, and scoped remind was 0/10); tool-text de-stall edits
(#200B falsified).

## The week's lanes, in order

### Lane 1 — #200I: spiralfix v2, event-scoped (FIRST; the direct #200H follow-up)

#200H proved the instructions seam tames the calendar lookup spiral
(calendar 9/10, zero casualties, hunts end in creates) but the sentence
bled across intents: "an event **or reminder**" doubled haiku grabs
(4/8 → 8/10) and sagged remind (6/10 → 4/10). Treatment: REWORD the
`lookupSpiralCarveout` text (it is flag-off and unpromoted — the text is
free to change; update its pin):

- Sentence 1, event-scoped: "A person's name in an event title is just
  part of the title — never search contacts, conversations, or places to
  identify them before creating the event."
- Sentence 2 unchanged: "Only include an event location the user
  themselves gave; a place search result is never the location."

Battery: `[.armed, .armedSpiralfix]` × 4 prompts × n=10 = 80 trials
(reuse the cell + a "Spiralfix battery n=10 (80)" style button or run the
spiral battery minus strikefix — executor's choice, pin whatever runs).
Bar: calendar ≥8/10 with zero casualties, remind ≥6/10 (back in band),
grabs ≤ control, misbind-clean creates > control. If it clears: promote
via the #200D/#200G pattern (default TRUE, pins flipped RED-first,
re-verify battery, Owen merges).

### Lane 2 — DeviceHealthTool throw audit (small, do early — it costs trials)

#200H lost 2 trials to `ToolCallError(tool: DeviceHealthTool …)` —
readHealth THREW on a haiku misroute. Belt honesty design: tools report
failure as their RESULT string, never throw. Audit `DeviceHealthTool.call`
(and every belt tool) for throwing paths; convert to explanatory strings;
TDD the deterministic parts. No battery needed; OPEN_ITEMS note + normal
PR.

### Lane 3 — strike-tally instrument probe (ONLY if #200I misses)

#200H's strikefix was INCONCLUSIVE: zero same-tool repeats occurred
in-cell (no third strike ever due), and the diagnostics are anomalous —
all strike emits doubled-at-#1, never #2, suggesting the
`@SessionProperty [String: Int]` write does not persist across
onToolCall invocations (unlike #200E's `Int`, which visibly counted
call#1 → call#2). Before ANY strike re-run: swap the tally to the PROVEN
Int machinery (total-call budget, demote at K=6 — relabel the cell
`armed-budget6`) or prove the dictionary persists. The `.disallowed`
mid-request decoder watch-for is still untested — if it trips the #200E
crash class, the cell is dead, route around, no filing.

### Lane 4 — the cap cell (D4 corruption, measured)

`maximumResponseTokens` is 1024 on-device (#102 latency tradeoff) and is
the standing suspect for the D4 ledger (missing-'title' ToolCallErrors,
comma-prefix titles, `Sam}<ctrl43>`, truncated/garbled replies — Apple's
own docs warn about cap-truncation). Cells: 1024 (control) / 2048 / nil,
armed construction, the 4 prompts, n=10 = 120 trials. Measure corruption
specimens per cell + latency deltas. Do NOT change #102's production cap
without this verdict.

### Lane 5 — grab-disease lane (design first, then battery)

~50% standing under armed construction. Candidates, in order of promise:
(a) an instructions carve-out licensing composition as the ANSWER
("writing the poem is the answer — never also create a reminder or event
about it" — planner-corpus style, one sentence, flag-gated); (b) accept
the router as the first line (haiku should route toolless; measure the
router's haiku miss rate before spending instruction budget). The #200H
meta-grabs (reminder titled "Write a haiku about sledding") are the
specimen class to kill.

### Stretch — DynamicProfile adoption bundle; Hermes-as-provider

Own dispatches when reached: (a) rollingWindow/historyTransform,
transcriptErrorHandlingPolicy, typed LanguageModelError handling for
production chat; (b) Hermes as a custom `LanguageModel` provider (App
Attest auth pattern — NEVER a bundled API key). Both documented in
`planning/FM-SDK-SEAM-SURVEY-2026-07-28.md`.

## The working rhythm (mechanical — follow exactly)

1. Branch `claude/t27-200<x>-<slug>` from `origin/main`, in the worktree
   (`.claude/worktrees/claude+t27-200-action-instrument`).
2. Dispatch doc in `dispatch/` (committed) for any multi-cell lane.
3. TDD: pins written FIRST and watched RED (compile-error red counts for
   new symbols; behavioral red needs a suite run —
   `-only-testing:TalariaTests/DeviceToolBeltTests` works at SUITE level;
   per-FUNCTION filters silently match nothing).
4. Full suite green WITH COUNT (last: **1296/1296 in 111 suites**):
   `DEVELOPER_DIR=/Applications/Xcode-beta4.app/Contents/Developer
   xcodebuild -project Talaria.xcodeproj -scheme Talaria -destination
   'platform=iOS Simulator,id=47F68496-24F9-45D9-93D3-1C778DB6B557'
   -only-testing:TalariaTests test` — run backgrounded, wait on
   `** TEST (SUCCEEDED|FAILED) **`, verify new-test names EXECUTED
   (stale-incremental trap: purge Intermediates.noindex if counts don't
   move).
5. File-scoped commits; OPEN_ITEMS notes as SEPARATE commits; commits end
   `Co-Authored-By:` (current model's line). PR body: what/tests/protocol.
   Merge commits only. Owen routes every merge.
6. Corded deploy: `git -C <main-checkout> fetch && git -C <main-checkout>
   checkout --detach origin/<branch>` → `RunProject(tabIdentifier:
   "windowtab1", attachDebugger: false)` (Xcode MCP) → Owen taps the
   battery button (corded, foregrounded, hands off — backgrounding kills
   the run). After merge: `git checkout --
   Talaria.xcodeproj/xcshareddata/xcschemes/Talaria.xcscheme` and restore
   `main`.
7. Classify from the bridge — `GetConsoleOutput(outputType: "stdio",
   pattern: "REAP |REAP-STEP|DONE|START|REFUSED")` for the seal, then
   per-cell `pattern: "shape=<cell> p=(remind|calendar|haiku)"`. Rates
   from RAW TEXT; a create = confirm=accepted + REAP-TRIAL artifact
   (replies LIE in both directions: created-then-denied AND
   narrated-reminder-that-was-an-event); ERROR/TIMEOUT excluded and
   listed; reap arithmetic exact (per-trial sums + backstop = totals =
   accepted creates; three consecutive runs were exact with ZERO backstop
   leftovers). Verdict as an OPEN_ITEMS note, dated, rates not verdicts.
8. NOTHING promotes without a verdict; promotions use the default-flip
   pattern with a pinned byte-identical rollback flag and a pooled
   re-verify battery.

## Owen-routing items (never decide these silently)

- Every merge, every promotion, every lane start beyond the next queued.
- Anything that would touch production behavior outside a measured flag.
- Apple bug filing: standing answer NO (verbatim: "the last time we
  thought we found an apple bug, it was our own oversight").
- #102's cap, tool descriptions, and the router are all frozen pending
  their own batteries.

## Do not relitigate (verified, some twice+)

Per-trial reap + unmarked echo work (3 runs, arithmetic exact, zero
leftovers, zero marker leaks). The corded flow works. The bridge
classification works (no console pastes needed). `xcodegen generate`
after add/remove of Swift files (none of #200F-H added files). The
`.required` crash. The `_Vision_FoundationModels` overlay lesson: sweep
every swiftinterface before declaring an SDK symbol absent
(`docs/reference/fmf-beta4-swiftinterface.md` is the vendored reference).
Memory files exist under the Claude project dir for the recurring traps
(SwiftData mainContext, device isolation, sim verify, beta4 stale
incrementals, FM SDK surfaces).

# WEEK PLAN 2026-07-30 → 2026-08-02 — the #200 program, corrected after #200T–#201

**Supersedes `WEEK-PLAN-2026-07-29.md` (Fable 5). That document's strategy was
sound and its lane order was followed; this one exists because three of its
mechanical instructions are now WRONG and would mislead the next reader, and
because its scoreboard predates a week of measurement.**

## Corrections to the 07-29 plan — read these first

| 07-29 plan said | Now known |
|---|---|
| `RunProject(… attachDebugger: false)` | **`attachDebugger: true`, always.** Four consecutive corded runs died mid-battery with no error; debugged processes are jetsam-exempt and the identical build then went 120/120 first try (#200V-era finding). |
| Suite 1296/1296 | **1337/1337 in 111 suites**, and lane runs use `-only-testing:TalariaTests` (the UI bundle's `testChatSendFlow` dies in bundle runs, passes in isolation). |
| Remind 75%, calendar ~70% | **Remind 20/20 pooled; calendar ~9/10 warm.** |
| Classify from the console bridge | **The run JSON is the system of record.** A console session expired mid-run and cost a verdict its arithmetic; both the confirmations and the reap seal were in the record the whole time. Use `scripts/classify-battery-run.py`. |
| (not stated) | **Warm-up on by default, production runs LAST.** Cold start was inflating every treatment arm — see below. |

## What production gained (all promoted, all with pinned rollbacks)

Instructions: destall (#200D) + find-first (#200G) + card-narration (#200K) +
dead-end carve-out (#200O). Schemas: reminder `due`/`list` optional (#200S),
calendar `durationMinutes`/`location` optional (#200X). Rollbacks: three Bool
flags whose explicit `false` is byte-identical, and two structs
(`ReminderCreateToolRequiredFields`, `CalendarEventToolRequiredFields`) reachable
as measured cells.

## The findings that outrank any single lane

1. **Cold start contaminated the instrument.** Same production config: calendar
   7/10 first-and-cold vs 9/10 last-and-warm; the FIRST cell posted the lowest
   calendar number in #200S, #200T and #200U. Warm-up is now default and
   production runs last (the conservative slot).
2. **And production is structurally ALWAYS warm** — every turn pays a router
   generation on the same model object before the real turn. So the cold-biased
   controls made every treatment look better than it was, *and* the warm numbers
   are the honest production estimate. The promotions survive this correction
   strengthened rather than qualified.
3. **Schema beats prose.** Five wording lanes failed against a stall that was a
   required-field contradiction. Two schema changes fixed remind and calendar.
   When behaviour resists instructions, look for a structural constraint saying
   the opposite.
4. **A hung tool cannot be cancelled, and production has no guillotine.** The 35s
   cancel is battery-only. That combination made an unbounded CoreLocation wait a
   ship blocker (#203, fixed).
5. **Bars get applied as written, including when they cost a result.** #200V's
   withdrawal trigger killed #200U; #200Z's primary missed by one trial and is
   filed unmet. That discipline is why these numbers mean anything.

## Lane status against the 07-29 plan

| 07-29 lane | Status |
|---|---|
| 1 — spiralfix v2 (#200I) | **RETIRED** — resurrects find-first (3 of 4 remind misses called `readReminders`). |
| 2 — health-tool throw audit | **Premise falsified, cost unresolved.** No belt tool throws; these are argument-DECODE failures above `call()`. Still costing trials (3 in #200W, 2 in #200Z, all `readHealth`). Fix shape is proven: strip required fields, as #200S/#200X did. |
| 3 — strike/budget probe | **DEMOTED.** Its trigger fired, but the spiral it targeted was cut structurally (`currentLocation` 7/10 → 0/10). Precondition still stands: swap the `@SessionProperty` dictionary to the proven `Int` machinery first. |
| 4 — the cap cell | **OPEN, and now the biggest one left.** See below. |
| 5 — grab disease | **CLOSED by measurement** — Fable's own option (b) won: router probe 200/200, canary routes toolless 20/20. |
| Stretch — DynamicProfile / Hermes-as-provider | Untouched. |

## Lanes, in priority order

1. **#201 — contact dead-end at n=20.** Built, bars pre-registered with an
   evaluability gate and a closing condition. Running 2026-07-30.
2. **#202 — the context-blind router. HIGH, and the highest-value measurement
   left.** The router classifies the raw current prompt with no history, its
   `@Guide` calls "conversation" false, and a routed-toolless turn registers NO
   belt — so "Yes please" after an offer has no `createReminder` and the flat
   denial is *correct* for that session. That is #200's own filing specimen. Every
   instrument we have is single-turn and blind to it. **Owed first: a two-turn
   offer→accept battery cell.** Our 20/20 remind scoreboard measures the half of
   the funnel that works.
3. **Lane 4 — the cap cell.** `maximumResponseTokens` 1024 / 2048 / nil × 4
   prompts × n=10. It is the standing suspect for the D4 corruption ledger AND
   plausibly the cause of the `readHealth` decode errors — which would make Lane 2
   and Lane 4 the same bug. Do not touch #102's production cap without this.
4. **#200K / #200O warm re-verifications.** The last two promoted clauses whose
   evidence predates the warm-up; both were measured control-first and cold.
5. **#203's residue** — production still has no turn-level deadline of any kind,
   and `ensureAuthorization()` parks forever if a permission dialog is dismissed
   without an answer. Both need a product decision about what a stuck turn should
   look like, not just code.
6. **`searchPlaces` needs n=20** before it can carry a bar (6/10, 1/10, 2/9).

## The working rhythm — changes from 07-29

Unchanged: branch in the worktree, dispatch doc with bars BEFORE data, pins
watched RED, file-scoped commits with OPEN_ITEMS notes separate, Owen routes every
merge/promotion/lane start, nothing promotes without a classified verdict, no
Apple filing ever.

Added by this week's experience:

- **Assert the compiled path**, not just the exit code — a cwd flip twice sent a
  run at the wrong checkout and produced a green suite for the wrong tree.
- **Bars need an evaluability gate and a closing condition**, or a measure that
  fails to show up reads as a pass. Size n to the MEASURE: counts and rates need
  different n, and a rate near its ceiling cannot carry a bar at n=10.
- **Deploy with the debugger attached; export the run JSON; classify with the
  script.**
- **NIGHT-ENDING EXTERNAL AUDIT (Owen's standing routine, 2026-07-29).** An
  independent reader (Hermes) audits the day's work cold. Its first two passes
  found a ship blocker and four real defects — **every one at a boundary the
  author had just worked on**: a classifier written to stop hand-tallying was
  keyed to one device's geography; a timeout built for hung tools skipped the
  provider three tools await. Self-audit cannot find these, because it checks the
  same reasoning with the same reasoning.

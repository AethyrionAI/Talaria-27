# 2026-09-04 — The launch audit's four P1s: execution plans

Owen's rulings tonight (09-04 evening, on the record in the handoff): **plans first, then this queue, lanes start Saturday.** Order of the plans, by harm: **#428 (A3, the hot microphone) → #427 (A2, the cross-conversation write) → #426 (A1, the transplant regression) → #429 (A4, Markdown image fetch).** A4's default is **tap to load**. Every plan opens with ONE AskUserQuestion round on its decisions, recommended arm first; the ballots are Owen's morning.

Method: the ten findings were already filed as #426–#435 (commit `1f0ef388`) with same-evening code verification; the audit is at `planning/reports/2026-09-04-launch-audit-astra.md`. Nothing was re-filed or re-verified. Each plan was written from a fresh read of the cited lines (every line number in every plan was read tonight, not recalled), the existing test fixtures each bar can extend, and the tracker entries the fix falsifies — in the memory plan's shape (rationale + global constraints + decisions with recommended defaults + pre-registered bars + a Task 0 that MEASURES the premises + RED-first tasks + a device card + a self-review).

## Plans written (each starts a NEW session; each opens with one AskUserQuestion round)

| plan | the defect (verified) | the touch | Task 0 measures | ballot |
|---|---|---|---|---|
| `planning/superpowers/plans/2026-09-04-428-voice-restart-outlives-shutdown.md` | `restartTask` is never cancelled or joined by teardown; the capture actor's start has no check after its four awaits and before install/start — a mic that can go HOT after End; #415's park path inherits it | a capture generation the controller checks after every suspension and before install (`.superseded`, one `.notice` line); teardown cancels AND joins the restart (3 s bound); the actor and its awaits go behind two seams so the ordering is a unit test driven through the REAL route-change notification | (a) hardware: five route-change-then-End trials on the CURRENT build — does the ordering reproduce? (b) sim: can the test host start the audio engine / clear the mic preflight / post the route change past the 750 ms cooldown | 4 decisions |
| `planning/superpowers/plans/2026-09-04-427-recovery-ownership-token.md` | `adoptRecoveredRun` writes into the LIVE `conversation` after an await with no ownership check; `reconcileInFlight` is cancelled nowhere; settlement then persists, journals and releases a held turn against the wrong thread | `RecoveryOwnership` (conversation id + session + run id + generation) captured BEFORE the status read, checked before EVERY write; walk-away and Stop cancel the in-flight pass and bump the generation; the legacy leg gets the same token; a witness enumerates the guarded writes | the fixture: which of the 23 call sites hosts a suspended mock (RunRecoveryClient), and the founding hazard — the protocol's default `openSession` returns A again, making a "switch to B" vacuous unless the mock implements it | 4 decisions |
| `planning/superpowers/plans/2026-09-04-426-transplant-prehistory-on-the-wire.md` | the wire history is built from the DISPLAY mapping, which replaces the primer's content with a notice and drops the ack; `runsHistory` then skips `.system` — the prehistory rides only the priming turn (a #330 regression, `faa725df`) | `fetchStoredMessages` + a stored-row wire mapper; the `[Message]` builder deleted; display byte-untouched; the 08-07 pin flipped RED-first; an integration bar inspects the next POST body | **ALREADY MEASURED tonight, read-only on the Mac gateway:** the host logs `history=len(conversation_history)` per turn (`agent/turn_context.py:797`); the first post-transplant turn reads `history=2` on 08-22/23/25 and `history=0` from 08-26 — the regression on the wire; one 08-31 anomaly assigned to the lane | 4 decisions |
| `planning/superpowers/plans/2026-09-04-429-markdown-images-tap-to-load.md` | `AsyncImage(url:)` inside the bubble's button fetches at render; the scheme is the whole gate; ATS permits any HTTPS; the HTML sandbox's block does not cover this plane | ONE `RemoteImageView` (the only `AsyncImage` site) gated by a per-launch, URL-keyed `RemoteImageConsent`; a placeholder naming the host; the bar is a hosted render with a counting `URLProtocol` and a live control arm; the privacy sentence rides the lane (held for Owen's read) | does a hosted `MarkdownContentView` issue `AsyncImage`'s request through a registered `URLProtocol` in the test host? (design A if yes; design B — an injectable loader — if not) | 5 decisions |

## The P2s and the prose items (no plans; bars can live in the entries)

- **#430 (A5)** recovery host, **#431 (A6)** share caps, **#432 (A7)** body logging — one "audit P2s" lane or three small lanes with bars pre-registered in their entries; #427's token gives #430 a natural home for a `profileID` if that lane wants one; #426's `fetchStoredMessages` moves #432's `.error` line unchanged and leaves its content to that lane.
- **#433/#434/#435** — prose/compliance with Owen's read: the policy page (`docs/` publishes on merge — Owen reads the exact sentences first; #429 proposes the image-origin sentence), the OFL notices + an in-app surface, WeatherKit attribution. Task lists, not code plans.

## What the four plans share (so a session can hold them to it)

1. A Task 0 that MEASURES the plan's premises before the bars are pinned — 426's is done; 428's has a hardware half (Owen's ~10 min) and a sim half; 427's and 429's are fixture-feasibility hours.
2. Bars pre-registered in the entry before code; a missed bar is a falsification, never a redefinition; RED-first with NAMED isolating mutations per bar; the RED witnessed on the unmodified tree, not assumed.
3. Each names a CONTRACT test across the composed path the audit found (restart → teardown; await → destination ownership; stored rows → POST body; markdown → network) — memory `composed-path-blind-spots`.
4. The close-out rule: every tracker block the fix falsifies (#128, #184, #226, #321, #330, #283, #259, #415, #302, #433) is named in the plan and gets its dated pointer in the same commit.
5. Gate on final bytes from the fixed sim pool, kill only your recorded PID, merge on green, RESULT block; device numbers carry build · `osVersion` · thermal.
6. Plan-authored code is unreviewed code: every sketch is an interface; each plan says which premise it could not verify from the desk and where that premise gets measured.

## State of the box tonight (for the morning session)

- The #219 DET-C/E batches were RUNNING on `CC-lane-2` while these plans were written (DET-C done 19:10, 0/10 red; DET-E n=20 in progress) — **no xcodebuild on the Mac until `det-ce.log` prints `DET-C/E done`.** None of these plans needs a build until its lane opens.
- The Mac gateway serves its checkout head (`71f8c60f6a`, listener started 17:51) — no drift; the 426 measurement above was taken against it read-only.
- `main` is clean; these plans, this index, the four entry pointers and one memory note are the only additions.

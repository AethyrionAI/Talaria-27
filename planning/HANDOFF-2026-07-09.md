# HANDOFF — 2026-07-09 session kickoff
*Written 2026-07-08 ~21:30 by Claude (Fable-tagged session). Read me first, then the live
OPEN_ITEMS.md (canonical, this repo). DC was last on the Mac; windowtab2 = this project in
Xcode-beta3; run destination was left on whoGoesThere (physical).*

## State (all pushed, verified)
- `AethyrionAI/Talaria-27` main @ `77329a9`. Contains: the full #44–#49 wave (six merges),
  two device-verified fixes (`17a7b0f` inbox 401-ladder gh#56, `81b160c` receipt cap gh#57),
  and three OPEN_ITEMS docs commits. Working tree clean.
- **OJAMD is canonical now (#36 ✅):** runs branch `ojamd-deploy` = t27/main + one cherry-pick,
  remote `t27` = this repo. Relay/gateway/connector all healthy at session end.
- **Inbox (#45/#80): verified end-to-end on device** — agent → connector → relay → tray.
  Owen saw both items. Internal key lives in `~/.hermes-mobile/secrets.json` on OJAMD.
- Phone relay session healthy (re-paired by Owen ~afternoon; refresh ladder observed working).

## PRIORITY 1 — #82 voice regression (T27 only)
Symptom: Talk setup works, relay mints OpenAI realtime session (200, no error), app calls
`POST /v1/talk/session/{id}/end` itself 2–37s later. Prime = healthy control (voice AND
voice-to-transcript work). Suspects: (1) Wave 5 audio work (#73/#74, merged 7/7), (2) beta-3
SDK relink (first device build tonight). Voice last worked 7/5 (pre-Wave-5, seed-1 SDK).

**Queued single-variable experiment (start here):**
1. `cd /Users/owenjones/Documents/Claude/Talaria-27 && git switch -c probe/pre-wave5 6820860`
2. Build with the SAME toolchain as tonight's broken build (Xcode-beta3 GUI Run to
   whoGoesThere, or CLI `env DEVELOPER_DIR=/Applications/Xcode-beta3.app/Contents/Developer`).
3. Test voice on device. Works → Wave 5 code convicted; broken → SDK relink convicted.
4. Then return to main. If Wave 5 convicted: diff the talk flow — `LiveVoiceSessionService`,
   TalkStore, and Wave 5's backend/brain router — for what triggers the self-teardown; add
   os_log at the end-call decision point if the error isn't already surfaced.

## Queue after #82 (or interleave)
- **#81 device checklist** (lock-screen Reply loop) — never run; relay half is deployed.
  Send from phone → lock → long-press push → Reply → verify it lands + next push has Reply.
- **#80 residual checks:** silent-push wake populating without manual refresh; approve →
  `get_inbox_verdict` readback; `notify="alert"` visible push.
- **One-click:** open the PR for branch `ojamd/update-hermes-helper` (already pushed).
- **gh#58 (open, future wave):** inbox decoder skip+log bad rows instead of failing the feed.
- **#83 optional:** try `UIRequiresFullScreen: true` for the Larger-Text letterbox; file
  Apple Feedback (repro is written in the item). Owen is on Default zoom meanwhile.
- **#14 (low, now unblocked by #36):** deploy canonical shim.py over OJAMD's interim patch.

## Hard-won rules from 2026-07-08 (also in memory + OPEN_ITEMS #55/#83)
- **#24f is DEAD** — relay is DB-backed, persistence verified live. Never cite it.
- **Record which Xcode seed built each device install** — check `DTXcodeBuild`/`DTSDKBuild`
  in the installed app's Info.plist before blaming app code for device-only "regressions."
  (Xcode-beta.app = seed 1 / SDK 24A5355p; Xcode-beta3.app = seed 3 / 24A5380g; phone = 380h.)
- Gateway config edits need the detached `pythonw` cycled (kill :8642 owner + relaunch
  `gateway-service\Hermes_Gateway.vbs`) — Hermes app restarts don't touch it. New MCP tools
  also need `tools.include` in HERMES_HOME config.yaml.
- Inbox kinds: only alert/approval/notification/reminder/suggestion. Never post other kinds
  via the raw `/internal/inbox/create` route — one bad row poisons the whole feed (gh#58).
- Keychain probes via RunCodeSnippet on a LOCKED phone return nil tokens — false negative.
- MEMORY NUMBERING STALE: memory says "#75 = voice regression"; in the live file #75 is the
  HUD-header item and the voice regression is **#82**. Trust the live file.

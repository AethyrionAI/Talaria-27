# Week plan — Wed 2026-08-19 → Sun 2026-08-23 (v2, post-sweep)

Rewritten Tuesday night after archive sweep 4 (73 formal closes; board now 99
entries, ~10 of them active build work; #368–#381 filed). Schedule rule from
Owen: **mornings = no device · evenings = device · weekend = unrestricted.**
Rulings already banked: 3E GO (#368) · App Lock GO Thu PM (#302/#323) ·
token-guard file+fix (#369) · doorbell = nothing for v1 · realtime key on both
hosts (#254-D/#303 runnable) · #342 invariants-only · #101/#109/#266 closed ·
**#173 route (a) ship-the-floor, (b) parked as watch · #340 route (a) app-side,
Friday · #257 CLOSED (3a-C passed on Owen's read, 22:15)** — board now 98.

Pending from Owen (any morning, no device): #375 config go (confirm — his
"9-approved" was ambiguous) · #325 forge route · #376 drain naming · #345
health-data question · #370 calendar glance.

## Wed 08-19
- **AM (no device):** #368 — the 3E cutover lane. Bars pre-registered in the
  entry before code; build through the morning. Parallel small lane: #365
  diagnosis (code read + sim repro). **Added per Owen: the #309 disposition
  brief** — the 16 surviving relay paths → adapt-or-delete dispositions
  (runs interface / talaria plugin / delete). The voice question is already
  RULED by direction (2026-08-18: adapt forward, never fall back to old
  processes) — the brief maps voice's re-home path like every other path.
  Owen reviews the table Thursday AM.
- **PM (device, ~15 min of phone):** ~~stage an OTA from main (it already carries
  #320/#321). Then the evening device minutes: the #349+#367 shared reopen
  check (60 s — closes both) · #350-D fixture (30 s — closes #350) · #345's
  Health-app glance · #370's calendar glance · delete #340's stray dateless
  reminder. Remainder of the evening: 3E continues.~~
  **ACTUAL (2026-08-19): the OTA went out in the MORNING (build 2848, `main @
  1a983144`) and 3E merged (`33108d05`) with four of five 3E-H steps device-passed.
  Owen's routing that evening: the remaining device minutes RIDE TO FRIDAY PM**
  (he stays up later to finish them) — **nothing in the list was blocking**, and
  Friday PM already carried both a cutover smoke and an explicit "anything left
  from Wednesday's minutes" slot. **Owed there:** 3E-H steps 3 (Stop, host-log
  confirmed) + 4 (leave the thread and return) · #349+#367 reopen · #350-D
  fixture · #345 Health glance · #370 calendar glance · #340's stray reminder.
  **The evening went to #369 instead** — built, gate PASS, PR #323 open.

## Thu 08-20
- ~~**AM (no device):** 3E gate + PR + merge on Owen's review; OTA re-stage with
  the cutover. **Owen rules on the #309 brief** (the decommission arc's last
  gate besides #310/#375). … If #375's go is
  confirmed: execute the Mac config retirement + open the dead-code deletion PR.~~
  **ALL OF THAT LANDED WEDNESDAY** — 3E merged `33108d05`, OTA build **2865**
  from `6d3512b1`, the #309 brief RULED a day early (all three questions), and
  #375 completed both halves (config retirement 08-18, deletion PR #324 merged).
  Thursday AM's written scope was empty by the time it arrived.
- **AM (no device) — ACTUAL:** **#310** — the `relayBaseURL`-optional lane.
  Its trigger (#368's merge) fired Wednesday; it is the first move for #309's
  twelve DELETE rows, and it is the CAUSE of #365's ~10 s profile-switch
  stall. Not on this plan as written; routed here by the 08-19 evening
  handoff. **Bars 310-A…F pre-registered in the entry before code**, and the
  one scope question ruled by Owen 08-20 AM: **the migration CLEARS existing
  profiles' relay URLs** (both hosts' relays are retired, so both profiles
  carry a dead URL).
- **Also ruled 08-20 AM: #383 is PULLED FORWARD to this week** — see Saturday
  item 8 below for the collision that forced it. Design pass first; the
  plugin half still needs Owen's per-experiment live-install go.
- Free bucket, unchanged and still unspent: #293(d) two-line fix + test ·
  #198B off-main AVAudioSession fix · #348's 10-minute Mac-side check ·
  **#173's never-claim floor** (ruled 08-18: ship (a); small, bars
  pre-register in the entry; carries #380's one-line Settings copy).
- **PM (device — voice slot, per Owen):** #302/#323 App Lock lane — the 302-C
  defer-until-unlock build, lock-state consultation for inference +
  `talaria_phone_query`, sim-verified, first device spot-checks same evening.

## Fri 08-21
- **AM (no device; iPad is home, so unattended #333 runs are fair game):**
  the #340 fix lane — **route (a) RULED 08-18**: `performCreate` resolves a
  bare clock time, plus the four-bucket scorer (correct / omitted /
  wrong-value / no-call) and the A/B on the runner. Also: #199A decline re-run
  (n=10, unattended); #372's rollback-arm run if time; ~~#369 token-guard lane
  if the morning stretches~~ (#369 landed Wednesday evening — PR #323).
- **PM (device):** #360 dictation pass (closes it) · #302/#323 closing bars ·
  #368 cutover smoke on the phone (one streamed turn + stop + reopen on the
  runs plane) · anything left from Wednesday's minutes.

## Sat 08-22 — the big device day (unrestricted)
Cheap-first, batched by setup:
1. #58 + #179 — cold-boot double-tap (30 s; closes #179, advances #58).
2. #312(b) + Z8 — gateway restart mid-turn (Mac-side bounce while streaming).
3. #329 / #330 — cold-launch misclassify + vanished SESSION block bars.
4. #254-D + #303 — realtime cold-launch checks (key now on both hosts).
5. #249F-D — one evening reminder → tonight-or-tomorrow question.
6. #124's seven App-Lock checks (folds into #302/#323 verification).
7. #162/#163/#165 re-confirms (1+2+2 bars) · #190's two checks · #61
   standalone title · #121/#122 resume + spend rows · #123 share-sheet walk ·
   #112 Comic Book live-switch · #77 URL-scheme taps · #33 Notes write.
8. #220 / #198A engine-pinned voice re-checks (two real calls, engine line
   quoted) · #222 image arm (opportunistic) · #332-c iPad fixture measurement.
   > **⚠️ COLLISION, ruled 2026-08-20 AM:** with both relays retired the
   > REALTIME engine cannot bootstrap (#383), so any realtime arm of these
   > bars is unrunnable as scheduled. Owen's call: **build #383 this week**
   > rather than re-scope Saturday. If #383 has not landed by Saturday, run
   > the native/local arms only and say so — do not score a realtime bar
   > against an engine that never started.
9. If at OJAMD: 241-E (2 min) · #148 NVIDIA prune · desktop-app relaunch
   (clears #346's last legacy child).

## Sun 08-23 — overflow
Formal closes of everything Saturday proved (per #261, dated blocks + archive
moves) · consolidate device evidence · next-week tee: #363's prune watch fires
~Tue 08-25 (`hermes talaria prune --dry-run` on the Mac).

## Watches this week
#236 (one more bundle-context firing → it gets a lane) · #344 · #358 · #359 ·
#324-W1/W2 · #219 · #241 (upstream PR #72739).

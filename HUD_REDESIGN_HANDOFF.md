# Talaria — HUD Redesign Handoff

_Session: 2026-06-21 · Branch: `claude/jolly-lovelace-jc032e`_

## TL;DR
The "Jarvis / Arc-Reactor" HUD redesign is applied across all screens and **builds
green on iOS 26, runs, and renders** (cyan reactor orb, mono telemetry labels, custom
fonts loading). It was generated by Claude Code from `design/Talaria.dc.html`; this
session fixed the defects that stopped it compiling and verified it on the simulator.
The two new features (model selector, sessions drawer) are **visual shells with stubbed
data** — backend wiring is Phase B (next).

## Where things are
- **Branch:** `claude/jolly-lovelace-jc032e` (pushed). Rename to `feat/hud-redesign` or
  merge to `main` once reviewed — NOT done yet.
- **`main`** has the pre-redesign app + this session's earlier fixes (SSE empty-reply,
  message persistence, pairing route, streaming timeout/false-fail, XcodeGen product-name
  + schemes pinning).
- HUD code: `Talaria/Core/Design.swift` (token layer), `Talaria/Core/HUD/*` (ReactorOrb,
  HUDComponents, HUDEffects), fonts in `Talaria/Resources/Fonts/` (9 ttf, registered via
  `project.yml` `UIAppFonts`).

## What this session fixed on the branch (committed here)
1. `Design.Colors.accentBright` ×6 → `Design.Brand.accentBright` (the token lives in
   `Brand`; touched 5 files).
2. `VoiceAttachmentSheet`: extracted the `attachmentRow(...)` view-builder method into a
   standalone `AttachmentRow: View` (Swift 6 isolation error — a `@MainActor` method's
   `some View` was returned across a nonisolated `PhotosPicker` closure).
3. Regenerated `project.pbxproj` so the HUD fonts are actually bundled. Claude Code edited
   `project.yml` but never committed a regenerated project, so its commit alone would have
   fallen back to system fonts.

## Phase B — wire the two shells to the backend (the next job)
Seams left by Claude Code:
- **Model selector** — `Features/Chat/Model/ModelSelector.swift` (`ModelSelectorModel`):
  - `availableModels: [ModelOption]` — `// TODO: wire to hermes config/models` (3
    placeholders). Populate from the host models endpoint.
  - `onSelectModel` — currently unset (local state only); connect to the real model-switch
    path. `activeModelNameOverride` is already live from `chatStore.activeModelName ??
    host.hermesModel`.
- **Sessions drawer** — `Features/Chat/Sessions/SessionsDrawer.swift` (`SessionsDrawerModel`):
  - `sessions: [SessionSummary]` — `// TODO: wire to Sessions API` (placeholders).
  - `onSelectSession` (no-op in `ChatScreen.configureChatSeams()` — should switch the active
    conversation), `onNewChat` (reuses existing clear/new flow), `onOpenHostSettings`
    (routed). `searchText` filtering is local-only.
- Header `SESSION · mm:ss` timer not added (no session-start timestamp source yet).

## To verify next session
- Header model chip reads **"GPT-5.5"** and the orb wordmark reads **"HERMES"** — confirm
  whether placeholder text or bindings to fix (live model name / app name "TALARIA").
- Tour the remaining screens on the sim (onboarding/handshake, talk/voice, inbox/directives,
  settings, the sessions-drawer + model-selector shells). Only the chat screen was
  screenshotted this session.

## Environment / how to build & run
- Repo: `/Users/owenjones/Documents/Claude/Talaria`. XcodeGen drives the project —
  `project.yml` is the source of truth; never hand-edit the `.xcodeproj`. Run
  `xcodegen generate` after any `project.yml` change (idempotent: a clean regen yields zero
  `git diff`).
- Build: scheme `Talaria`, destination `id=47F68496-24F9-45D9-93D3-1C778DB6B557`
  (iPhone 17 Pro Max, iOS 26). Product is `Talaria for Hermes Desktop.app` in DerivedData
  `Talaria-bkmofmhhchhruzcdudrizbbblrae`.
- **Deploy gotcha:** installing over a running app only foregrounds the OLD process. Always
  `terminate → install → launch` to load new code. idb keyboard text injection does NOT
  work in this simulator — the user types chat messages manually.
- Runtime context: the relay/host is OFFLINE; chat goes DIRECT to the local Hermes API at
  `localhost:8642`. The "Hermes host offline" banner + stale model chip are relay-sourced
  and expected in this mode.
- Tooling available on the Mac: Desktop Commander (shell), `gh` (authed as ChronoRixun),
  `xcodegen` 2.45.4, the xc-all simulator tools.

## Open decision
Branch is `claude/jolly-lovelace-jc032e` (harness-mandated name). Once the redesign is
reviewed/approved: rename to `feat/hud-redesign` and/or merge into `main`.

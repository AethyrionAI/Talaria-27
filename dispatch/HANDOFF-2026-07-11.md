# HANDOFF — 2026-07-11 (supersedes HANDOFF-2026-07-10.md)

**Repo:** AethyrionAI/Talaria-27 `main` @ 75dd6b3, clean. Phone (whoGoesThere) keeps c9e909e.
**Fable credits expire SUNDAY** — dispatch the lanes below in parallel and let them run.

## What this session did (2026-07-09 late)
- **P1 transplant-fidelity probe: PASS -> Lane A GO** (OPEN_ITEMS #89). Three-arm probe vs the
  `:8642` Sessions API; a condensed ~10:1 priming turn was indistinguishable from the original
  session and a raw replay on recall, cross-turn inference, and a mid-stream correction. A second
  "condenser-fidelity" rung (machine condensation of a messier 2-correction / 2-distractor
  transcript) also passed on fidelity; the residual is pruning / token discipline, and the
  on-device `LocalIntelligenceService` condenser is still the real untested proxy — folded into
  Lane A as an acceptance test.
- **OJAMD Priority 3 fully closed:** #87 connector utf-8 DEPLOYED (rebased `ojamd-deploy` onto
  `t27/main`, fix live on the editable module, connector reattached to relay, `hermes memory
  status` healthy); #88 `restart-relay.ps1` FIXED (relay half -> `Restart-Service
  HermesMobileRelay`; the script lives in `~/.hermes/scripts/`, untracked by design); #54
  VERIFIED-resolved (host-connection nonce is DB-persisted + race-safe eviction in
  `relay/app/services.py`; zero 4401 on reattach).
- **Owen closed:** Discord (token in `.env`, bot invited) and #38 APNs (config in place, tests pass).
- **OPEN_ITEMS updated** on `main` working tree: #38 / #54 / #87 / #88 -> resolved, Discord closed,
  #89 added. (Commit was pending Owen at handoff time.)

## THE PLAY — dispatch Fable (all three in parallel, separate branches)
- **Lane A** — `dispatch/FABLE-LANE-A-continuity-fabric.md`. GREENLIT, biggest spend; carries the
  condenser-fidelity acceptance test (the probe's one open risk).
- **Lanes B + C** — `dispatch/FABLE-LANES-BC.md`. B = markdown rendering depth; C = correctness
  batch (`/save` silent-failure, unused `HapticEngine.error()`, stale settings comment, #58 inbox
  decoder, #84 preflight third-state). Both probe-independent — fire anytime.
- **Lane D** (only if credits remain): P8 IR v0, OR candidates #5 (tool telemetry, P7 prereq) /
  #11 (composer draft persistence).
- **Merge sequencing (Mac-side):** C before A (`ChatScreen.swift` overlap); C item 5 stays on the
  `claude/t27-84-talk-preflight` branch; B independent. All spec line refs confirmed live at 75dd6b3.

## Parked / standing
- **#82 voice wedge PARKED** — Apple beta seed breaks ALL third-party capture (Discord repro'd it,
  Talaria-free). Retest = ~10 min on the next seed; Owen owes Apple Feedback.
- **#84 branch** (`claude/t27-84-talk-preflight` @ c9e909e) UNMERGED — owes Lane C item 5 + the
  wedge-blocked device checklist.
- **#85** hermes_delegate URL (`PUBLIC_BASE_URL` is Tailscale-only, unreachable from OpenAI) —
  architectural, needs Owen's exposure decision. **#86** QueuePool quiescent (2 hits ever, not
  recurring; the incoming commit was an audit, no code fix to deploy).
- **Strategic fork:** standalone App Store app vs the continuity vision — this session's probe
  leaned continuity.
- **Toolchain:** ALWAYS `export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer`
  (default CLI xcodebuild = 26.6 = wrong-SDK ghost errors).
- **Probe harness** reusable at `talaria-probe/probe.py` (OJAMD) for the on-device
  condenser follow-up rung.
